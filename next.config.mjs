/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'www.former.lol',
          'onlocklearning.vercel.app',
          'www.usatii.com',
          'usatii.com',
          'usatii.com/theopenpaper'
        ],
      },
};

export default nextConfig;
